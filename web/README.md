# Market Lens website

Next.js app deployed to Vercel. See the project docs:

- Setup and commands: [../docs/setup.md](../docs/setup.md)
- How it fits together: [../docs/architecture.md](../docs/architecture.md)

Quick start (after copying `.env.example` to `.env.local` and filling it in):

```powershell
npm install
npm run dev
```

Open http://localhost:3000
